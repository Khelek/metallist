= simple_form_for @notification, url: admin_notifications_path, html: { class: 'form-horizontal' } do |f|
  = f.input :title
  = f.input :body
  .form-actions
    = f.button :submit, class: 'btn-primary'
    = link_to t('.cancel', default: t("helpers.links.cancel")), admin_notifications_path, class: 'btn'
