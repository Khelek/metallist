- model_class = Notification
.page-header
  %h1=t '.title', default: model_class.model_name.human.pluralize.titleize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:title)
      %th= model_class.human_attribute_name(:body)
      %th= model_class.human_attribute_name(:created_at)
      %th=t '.actions', default: t("helpers.actions")
  %tbody
    - @notifications.each do |notification|
      %tr
        %td= notification.title
        %td= notification.body
        %td=l notification.created_at
        %td
          = link_to t('helpers.edit', default: t("helpers.links.edit")), edit_admin_notification_path(notification), class: 'btn btn-mini'
          = link_to t('helpers.destroy', default: t("helpers.links.destroy")), admin_notification_path(notification), method: :delete, data: { confirm: t('.confirm', default: t("helpers.links.confirm", default: 'Are you sure?')) }, class: 'btn btn-mini btn-danger'

= link_to t('helpers.new', default: t("helpers.links.new")), new_admin_notification_path, class: 'btn btn-primary'
